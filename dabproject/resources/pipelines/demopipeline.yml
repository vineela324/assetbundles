resources:
  pipelines:
    pipeline_demopipeline:
      name: demopipeline
      libraries:
        - glob: #glob = include everything under this path that matches the pattern.#
            include: ../../src/pipelines/demopipeline/transformations/**
      schema: bronze
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog_name}
      serverless: true
      root_path: ../../src/pipelines/demopipeline
